# Basu-д хувь нэмэр оруулах

Энэ файл кодыг биш, **шийдвэрүүдийг** тайлбарлана. Юу хаана байгааг `README.md`
хэлнэ; яагаад тэгж байгааг код доторх тайлбарууд хэлнэ. Энд зөвхөн шинэ хүн
эхний өдөр мэдэх ёстой зүйлс байна.

## Эхлүүлэх

macOS, Postgres 16, Node 22. Docker хэрэггүй.

```bash
createdb basu_dev basu_test
cp .env.example .env          # DATABASE_URL-ээ шалгана уу
npm install

npm run verify                # бүх зүйлийг нэг командаар — эхлээд үүнийг ажиллуул
npm run dev                   # миграц + демо өгөгдөл + сервер
```

`npm run verify` цэвэр checkout дээр өнгөрөх ёстой. Өнгөрөхгүй бол код биш,
орчин чинь эвдэрсэн байна — эхлээд түүнийг зас.

iOS дээр ажиллах бол нэмээд:

```bash
brew install xcodegen
npm run ios                   # төсөл үүсгэж, build хийж, симулятор дээр асаана
npm run ios:test              # unit + бүтэн урсгал (сервер ажиллаж байх ёстой)
```

Xcode төсөл git-д байхгүй. `ios/project.yml` бол эх сурвалж, `xcodegen` төслийг
үүсгэнэ — үүсгэсэн файл нь файл нэмэх бүрд өөрчлөгддөг, merge нь муу байдаг.

## Гурван дүрэм

Эдгээрийг зөрчсөн PR буцна. Гурвуулаа кодоос харагдахгүй тул энд бичсэн.

### 1. Модуль нөгөөгийнхөө хүснэгтэд хүрэхгүй

Модуль бүр өөрийн Postgres schema-тай: `identity`, `ledger`, `notify`, `dine`.
Хэрэгтэй зүйлээ нөгөө модулиасаа **`index.ts`-ээр нь асууна**, JOIN хийхгүй.

```ts
// үгүй
const { rows } = await db.query('SELECT phone_e164 FROM identity.guest WHERE id = $1');

// тийм
const contacts = await contactsFor([guestId]);
```

`src/test/boundaries.test.ts` энэ заагийг барина. Унавал жагсаалтыг тэлэх биш,
асуух нь зөв засвар. Шалтгаан нь: тэр өдөр модулийг өөрийн сервис болгож
таслах шаардлага гарахад хэн нэгний бичсэн JOIN бүрийг олох боломжгүй болно.

### 2. Мөнгө бол давхар бичилт

`ledger` дотор хаана ч `balance` багана байхгүй бөгөөд байх ч ёсгүй. Үлдэгдэл
гэдэг нь дансны бичилтүүдийн `SUM(amount_mnt)`. Алдааг засах арга нь **эсрэг
гүйлгээ бичих**, хуучныг өөрчлөх биш.

Vertical нь QPay-тэй ярихгүй. `collect()` дуудна, ledger нь үлдэгдлээс хасах уу
эсвэл зөрүүг картаас татах уу гэдгийг өөрөө шийднэ.

### 3. Цаг бол параметр

`src/domain/` дотор `Date.now()` гэж байхгүй. Бүх зүйл `Clock` авдаг. Гал тавих
цагийн тухай систем цагийг хатуу бичсэн бол тестлэх аргагүй, демо нь өдрийн
дөрвөн цагт ажиллахгүй.

## Тестийн давхаргууд

| Давхарга | Юуг батлах | Хэзээ ажиллуулах |
|---|---|---|
| `npm test` | Арифметик, төлөв, модулийн зааг, мөнгөний уралдаан | Байнга |
| `npm run sim` | Оргилын ачаалал даана | Төлөвлөлт хөндвөл |
| `npm run smoke` | HTTP-ээр, бодит сервер дээр | API хөндвөл |
| `npm run verify` | Дээрх бүгд, зөв дарааллаар | **PR нээхийн өмнө** |
| `npm run ios:test` | Swift unit + симулятор дээрх бүтэн урсгал | Swift хөндвөл |

CI нь `npm run verify`-г Postgres 16 дээр ажиллуулна. iOS-ийг ажиллуулахгүй —
macOS runner үнэтэй, тэр багц зөвхөн Swift хөндөхөд эвддэг. Swift хөндсөн PR
нээхийн өмнө `npm run ios:test`-ээ өөрөө ажиллуул.

## Схем өөрчлөх

Миграц нь дугаарлагдсан `.sql`, зөвхөн урагш, **зөвхөн нэмэх**. Багана устгах
нь өөрийн гэсэн дараагийн релиз — тэгвэл буцаахад өгөгдөл зохиох шаардлагагүй.

Schema хооронд гадаад түлхүүр **битгий** нэм. Багана нь нөгөө талын id-г
заадаг ч FK биш; тэр гагнаас байвал модулийг таслах өдөр production дээр
яаралтай тайрах болно.

## Демо цаг

Үйлчилгээ 11:30–14:00-д явагддаг, галлах ба суух хоёрын хооронд 15 минут
байдаг. Тиймээс бүтээгдэхүүнийг өдрийн 4 цагт бодит цагаар үзүүлэх боломжгүй.
Хөгжүүлэлтэд цаг бол удирдлага: хуудасны дээд талын зурвасаас цагийг үсэргэж,
scheduler-ийг гараар гүйлгэнэ. Production-д `src/entry/*` нь `systemClock`
ашиглах тул тэр зурвас огт гарахгүй.

## Хэл

Хэрэглэгчийн харах бүх текст монгол кирилл. Кодын тайлбар, commit, хувьсагчийн
нэр англи. Алдааны текст `src/api/errors.ts`-д нэг дор байдаг — нэг нөхцөлийг
гурван газар гурван янзаар тайлбарлахгүйн тулд.

## Commit

Юу өөрчилснийг биш, **яагаад** гэдгийг бич. Юу өөрчилснийг diff хэлнэ.
Аль хувилбарыг сонгосон, юуг нь хаясан, ямар үнээр гэдгийг л хүн дараа нь
diff-ээс уншиж чадахгүй.
